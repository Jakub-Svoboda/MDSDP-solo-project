layout main: // visualised in basic.html
    vertical:
        form validate: // forms can have the optional validate flag
            label Username
            email // allow custom validation rules using regex? each premade element has a default validation rule that can be overridden
            label Password
            password 
            button submit { id=login size=large; label=Login } // submit to indicate that it will complete the form. label=Login could be inferred from teh name of the button
        button { id=register; size=medium; label=register }